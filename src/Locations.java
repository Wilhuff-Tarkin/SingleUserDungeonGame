import Map.MapTile;

import java.util.LinkedList;
import java.util.List;

public class Locations {
    List lokacje = new LinkedList();

//    MapTile otchlan = new MapTile(0, "nic tu nie ma", null, null, null, null);
//    MapTile start = new MapTile(1, "to jest lokacja startowa, jest tu ciemno i straszno", null, null, null, null);

}
